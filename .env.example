# Database Configuration
DB_USER=root
DB_PASSWORD=your_secure_password_here
DB_HOST=localhost
DB_PORT=3306
DB_NAME=expense_tracker

# Security
SECRET_KEY=your_very_long_and_random_secret_key_here_change_this_in_production
ACCESS_TOKEN_EXPIRE_MINUTES=30

# For production, generate a strong secret key using:
# python -c "import secrets; print(secrets.token_urlsafe(32))"
